---
import Layout from "../layouts/Layout.astro";
---

<Layout title="XX电脑科技｜电脑及周边销售与企业IT服务">

  <!-- Banner -->
  <section class="hero">
    <h1>电脑及周边产品 · 企业 IT 技术服务</h1>
    <p>
      专注电脑整机、配件销售，为企业与个人提供稳定、高效的技术支持解决方案
    </p>
    <a href="/services" class="btn">咨询技术服务</a>
  </section>

  <!-- 核心服务 -->
  <section class="services">
    <div class="card">
      <h3>🖥 电脑与配件销售</h3>
      <p>台式机、笔记本、显示器、办公设备一站式采购</p>
    </div>
    <div class="card">
      <h3>🏢 企业 IT 方案</h3>
      <p>办公网络、设备部署、IT 外包与技术支持</p>
    </div>
    <div class="card">
      <h3>🛠 运维与维修服务</h3>
      <p>系统安装、网络维护、上门维修、远程支持</p>
    </div>
  </section>

  <!-- 应用场景 -->
  <section class="scenes">
    <h2>服务对象</h2>
    <ul>
      <li>中小企业办公环境</li>
      <li>门店 / 商铺电脑部署</li>
      <li>家庭及个人用户</li>
      <li>政府 / 教育单位</li>
    </ul>
  </section>

  <!-- CTA -->
  <section class="cta">
    <h2>需要专业电脑与技术支持？</h2>
    <p>欢迎联系我们，获取定制化解决方案</p>
    <a href="/about" class="btn light">联系我们</a>
  </section>

  <style>
		:root {
	--blue: #2563eb;
	--blue-dark: #1e40af;
	--gray: #475569;
	--bg-light: #f8fafc;
	}

	/* Banner */
	.hero {
	padding: 120px 24px;
	text-align: center;
	background: linear-gradient(
		135deg,
		#1e3a8a,
		#2563eb
	);
	color: #fff;
	}
	.hero h1 {
	font-size: clamp(32px, 5vw, 48px);
	margin-bottom: 20px;
	}
	.hero p {
	font-size: 18px;
	max-width: 720px;
	margin: 0 auto 40px;
	opacity: 0.9;
	}

	/* 按钮 */
	.btn {
	display: inline-block;
	padding: 14px 36px;
	background: #fff;
	color: var(--blue-dark);
	border-radius: 999px;
	text-decoration: none;
	font-weight: 600;
	transition: all .2s ease;
	}
	.btn:hover {
	transform: translateY(-2px);
	box-shadow: 0 10px 20px rgba(0,0,0,.15);
	}
	.btn.light {
	background: transparent;
	color: var(--blue);
	border: 2px solid var(--blue);
	}

	/* 服务卡片 */
	.services {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
	gap: 32px;
	margin-top: 96px;
	}
	.card {
	background: #fff;
	padding: 32px;
	border-radius: 16px;
	box-shadow: 0 10px 30px rgba(0,0,0,.05);
	transition: all .25s ease;
	}
	.card:hover {
	transform: translateY(-6px);
	box-shadow: 0 20px 40px rgba(0,0,0,.08);
	}
	.card h3 {
	margin-bottom: 12px;
	}

	/* 场景 */
	.scenes {
	margin-top: 120px;
	text-align: center;
	}
	.scenes h2 {
	font-size: 32px;
	}
	.scenes ul {
	list-style: none;
	padding: 0;
	margin-top: 32px;
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
	gap: 16px;
	}
	.scenes li {
	padding: 16px;
	background: var(--bg-light);
	border-radius: 12px;
	}

	/* CTA */
	.cta {
	margin-top: 120px;
	padding: 96px 24px;
	background: linear-gradient(
		135deg,
		#f8fafc,
		#eef2ff
	);
	text-align: center;
	}
	.cta h2 {
	font-size: 32px;
	margin-bottom: 16px;
	}
	.cta p {
	color: var(--gray);
	margin-bottom: 32px;
	}

  </style>

</Layout>
